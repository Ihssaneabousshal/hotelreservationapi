pipeline {
    agent any
    
    stages {
        stage('Test GitHub Connectivity') {
            steps {
                echo 'Testing GitHub connectivity is it workin in private!'
            }
        }
    }
}
